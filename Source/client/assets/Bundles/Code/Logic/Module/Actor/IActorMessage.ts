import { IMessage, IRequest, IResponse} from "../Message/IMessage";

// 不需要返回消息
export interface IActorMessage extends IMessage {
    
}

export interface IActorRequest extends IRequest {
    
}

export interface IActorResponse extends IResponse {
    
}